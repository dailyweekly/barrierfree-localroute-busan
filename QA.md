# 심사위원 예상 Q&A — 배리어프리 로컬루트 부산

## Q1. 공공데이터 활용성 (30점) — "공공데이터가 진짜로 필요한가?"
**A.** 부산 도시철도 안에서 "실제로 이동 가능한 경로"와 "부산 관광·로컬상권 목적지"를 동시에 연결하려면 부산교통공사·부산관광공사 공공데이터가 필수입니다. 코드상 `lib/routing/score.ts`는 학습라벨·경로복잡도·대체경로유형 3개 컬럼을 모델 정답으로 직접 인입합니다. 공공데이터 없이는 알고리즘 자체가 동작하지 않습니다.

## Q2. AI 기술 (15점) — "LightGBM·딥러닝은 안 쓰나?"
**A.** 932행 규모 데이터에 딥러닝은 과적합 위험이 큽니다. 본 서비스는 **규칙기반 점수화 + 경량 분류기 + K-fold 교차검증 + 설명가능 AI + LLM 설명**을 결합한 의사결정 지원형 모델로 설계했습니다. 평가는 `lib/eval/metrics.ts`에서 F1·Macro F1·MAE·RMSE·Spearman·Top-1 성공률을 실측합니다. 2단계 사업화에서 LightGBM·SHAP를 추가 도입하는 로드맵입니다.

## Q3. LLM 환각·안전성 — "LLM이 헛소리하면?"
**A.** LLM은 **경로를 결정하지 않습니다**. 입력 화이트리스트(`buildWhitelistedInput`)로 모델이 선택한 후보만 LLM에 전달하고, 출력 템플릿 고정(복잡도/예상혼잡/대체)·존재 검증(`knownStations`)·이동불가 추천 차단·금지표현 검출의 5중 가드레일을 통과해야 화면에 노출됩니다. 거부 시 결정론적 폴백을 사용합니다. 가드레일 통과·거부 로그는 `lib/llm/log.ts` ring-buffer로 누적되어 `/api/metrics`에서 환각 검출률로 확인 가능합니다.

## Q4. 실시간 데이터가 없잖아?
**A.** 사실입니다. 공공데이터 5종은 정기 갱신형 스냅샷이며, 본 서비스는 **"정기 갱신형 설명가능 AI 추천 서비스"** 로 정확히 포지셔닝합니다. "실시간 혼잡 예측·실시간 고장 감지" 표현을 절대 사용하지 않습니다 (`scripts/check_forbidden_terms.ts` 자동 점검). 2단계에서 부산교통공사·민간 지도·시설 장애신고 API와 연계해 실시간성을 단계적으로 고도화합니다.

## Q5. AI 서비스 (15점) — "완결성?"
**A.** 5개 화면이 한 흐름으로 연결됩니다: 입력 → 3개 경로 비교 → AI 설명 → 로컬루트 확장 → 관리자 대시보드. 큰글씨·고대비·쉬운말·음성안내(TTS 호환 평문)·아이콘+텍스트·ARIA 라벨 등 접근성 8요소를 모두 구현했습니다.

## Q6. 독창성 (15점) — "유사 서비스가 있지 않나?"
**A.** 비짓부산·네이버지도·카카오맵·부산 스마트시티 배리어프리 내비와 비교표를 `lib/business/competition.ts`에 코드화했습니다. 본 서비스의 3대 독창성: (1) 공공데이터 학습라벨 기반 AI 의사결정 (2) 이동약자 조건 + 예상혼잡 + 로컬 목적지 동시 추천 (3) 설명가능 AI + 환각 방지 LLM. 어느 기존 서비스도 이 세 요소를 동시에 다루지 않습니다.

## Q7. 발전가능성 (20점) — "사업화 가능한가?"
**A.** B2G(지자체·교통공사 SaaS), B2B(MICE·호텔·여행사 동선 패키지·API), B2C(고령자·휠체어 이용자·가족관광객 프리미엄), 로컬상권(택슐랭·유니크베뉴 송객) 4축으로 매출 다각화 설계. 수익모델 5종(SaaS·MICE 패키지·관광 API·B2C 프리미엄·데이터 리포트)이 코드(`lib/business/markets.ts`)에 명시되어 있고 단계별 로드맵(`lib/business/roadmap.ts`)이 4단계로 분리되어 있습니다.

## Q8. ESG (5점) — "정량 지표는 어떻게 측정?"
**A.** 6개 KPI 중 4개는 시나리오 20종 결과로 자동 산정됩니다(`app/api/metrics/route.ts` esgKpiResults). 교통약자 도달 성공률·이동불가 추천율·안전경로 평균 복잡도·로컬상권 연결률. 나머지 2개(혼잡 회피 효과·설명 이해도)는 사용자 패널 테스트 단계에서 산정 예정으로 정직하게 표기.

## Q9. 안전 책임 — "이동불가 경로를 추천하면 어떻게?"
**A.** 안전성 게이트가 코드 단위로 강제됩니다. ① `scoreRow()`에서 passable=false인 행은 결합 점수에 +999 페널티로 사실상 추천에서 제외 ② 시나리오 평가에서 `expected.forbiddenTypes: ["이동 불가"]` 위반 시 시나리오 실패 처리 ③ 대시보드의 "이동불가 경로 추천율" KPI 목표 0%.

## Q10. 개인정보 — "GPS·계정 정보 수집?"
**A.** MVP는 개인식별정보 비수집입니다. 사용자 입력은 익명 시나리오 데이터로 처리하며, `.env.local` API 키는 서버 사이드에서만 사용되어 클라이언트 번들에 노출되지 않습니다(`server-only` 마크로 강제). 위치추적 미사용.

## Q11. 데이터 출처·라이선스
**A.** 5종 모두 공공데이터포털 라이선스(이용허락범위 제한 없음·CC BY)를 준수하며, 각 데이터의 ID·URL·제공기관·라이선스·갱신주기·컬럼수·활용위치·리스크가 `lib/data/sources.ts` DATA_SOURCES에 코드 상수로 명시되어 있습니다. `/api/metrics`와 `/api/recommend` 응답에 자동 포함됩니다.

## Q12. 후원기관상 (부산교통공사·부산관광공사)
**A.** 두 기관 데이터를 알고리즘 핵심과 서비스 확장의 핵심에 모두 활용합니다. (1) 부산교통공사 — 엘리베이터 대체경로(`scoreRow`)와 시간대별 승하차인원(`estimateCongestion`) (2) 부산관광공사 — 유니크베뉴(`/api/local-poi`). 서식6 증빙은 코드의 `usageTrace`/`sourceCells` 응답으로 자동 추출됩니다.
