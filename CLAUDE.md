# CLAUDE.md — 배리어프리 로컬루트 부산 (Next.js · Vercel)

## 0. 프로젝트 정체성
- 이 프로젝트는 "배리어프리 로컬루트 부산"이며, 부산교통공사·부산관광공사 공공데이터를 활용해 교통약자·관광약자의 도시철도 이동 의사결정을 돕는 정기 갱신형 설명가능 AI 추천 서비스이다.
- 공모전 MVP와 사업화 트랙을 하나의 Next.js(App Router) 코드베이스에서 단계적으로 키워간다. 배포 타깃은 Vercel.

## 1. 금지 표현 / 권장 표현
금지: "실시간 엘리베이터 고장 감지", "실시간 혼잡(도) 예측", "BTS 공연 대비", "AI 완전 자동 최적경로", "교통약자 문제 완전 해결", "100% 안전", "완벽한 경로", "데이터 검증 없이 확인됨".
권장: "정기 갱신형 AI 추천 서비스", "엘리베이터 이용불가 상황 가정 대체경로 안내", "시간대 기반 예상혼잡 산정", "검증된 대형이벤트 수요에 대응", "설명가능 AI 기반 경로 추천", "교통약자 이동 의사결정 지원".

`npm run check-forbidden` 으로 코드·문서 전체 자동 점검.

## 2. 핵심 공공데이터 (Plan A 확정 — 2026-05-18 Swagger 검증)
1. 부산교통공사_엘리베이터 고장 시 대체 이동 경로 (15151579) — 23개 컬럼, 핵심 라벨: 경로 이용 가능 여부 / 대체경로유형 / 경로복잡도 점수·등급 / 학습라벨.
2. 부산교통공사_시간대별 승하차인원 (3057229) — 역·요일·시간대 24개. 월간 갱신. **실시간 예측 아님**.
3. 부산관광공사_유니크베뉴 (15156491) — 연번·베뉴명·위치.
4. 부산광역시_갈맷길 코스 정보 (15077591) — REST API.
5. 부산광역시_택슐랭 선정 식당 (15143578) — 위치·식당명·분류·주소·추천메뉴·위경도.

## 3. AI 설계 원칙
- 규칙기반 + 경량 모델 + 설명가능 + LLM 설명 레이어 조합. LLM은 경로를 결정하지 않는다.
- LLM 출력은 5중 가드레일을 통과해야 한다: ①입력 화이트리스트 ②출력 템플릿 고정 ③존재 검증 ④이동불가 추천 차단 ⑤"확인 필요" 폴백.

## 4. 저장소 구조
- app/ — App Router 페이지·Route Handlers
- lib/ — 데이터·라우팅·혼잡·LLM 모듈
- components/ — UI 컴포넌트
- data/sample/ — 실API 구조와 일치하는 폴백 샘플
- scripts/ — 금지표현 점검·데이터 사전수집
- tests/ — Vitest/Node 내장 테스트

## 5. 개발 원칙
Explore → Plan → Implement → Verify → Commit. 작은 단위 커밋.
큰 변경은 한 번에 하지 않는다. 데이터 컬럼을 확인하지 않고 확정 표현을 쓰지 않는다.

## 6. 보안·개인정보
- `.env` 절대 커밋 금지. `.env.example` 만 커밋.
- API 키·서비스키는 서버 사이드(Route Handler)에서만 접근. 클라이언트 번들에 노출 금지.
- MVP는 개인식별정보 비수집.

## 7. 테스트·검증
- `npm run typecheck` / `npm run lint` / `npm run test` / `npm run check-forbidden` 4종 모두 통과.
- 시나리오 20종은 어드민 대시보드에서 통과율로 표시. 이동불가 경로 추천 0% 가 안전성 게이트.

## 8. 완료 기준
변경 파일, 구현 기능, 검증 방법(테스트·캡처·성능지표), 남은 리스크, 다음 작업 제안을 항상 함께 보고.
